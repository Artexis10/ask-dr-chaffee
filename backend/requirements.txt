# Core dependencies
psycopg2-binary>=2.9.0
python-dotenv==1.0.0
requests>=2.28.0

# YouTube transcript extraction
youtube-transcript-api>=0.6.0
yt-dlp>=2023.10.0

# Text embedding and processing (lighter versions)
sentence-transformers>=2.2.0
numpy>=1.24.0

# Utilities
webvtt-py>=0.4.0
beautifulsoup4>=4.12.0
lxml>=4.9.0

# Optional: Audio transcription fallback (install separately if needed)
# faster-whisper>=0.10.0
# torch>=2.3.0
# torchaudio>=2.3.0

# Note: For Zoom API, we'll use direct requests instead of pyzoom
